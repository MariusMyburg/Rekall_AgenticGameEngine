<Window x:Class="Rekall.Age.Studio.CreateProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Project" Width="560" Height="430" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource WorkbenchBackground}" Foreground="{StaticResource WorkbenchText}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="NEW REKALL AGE PROJECT" Style="{StaticResource SectionLabel}" Foreground="{StaticResource Accent}" />
            <TextBlock Text="Create a clean home for your game" FontSize="24" FontWeight="SemiBold" Margin="0,5,0,5" />
            <TextBlock Text="Studio will create the project folder and its initial scene." Foreground="{StaticResource MutedText}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="PROJECT NAME" Style="{StaticResource SectionLabel}" Margin="0,0,0,6" />
            <TextBox x:Name="ProjectNameBox" Text="New Rekall Game" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="LOCATION" Style="{StaticResource SectionLabel}" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBox x:Name="ParentFolderBox" ToolTip="The project folder will be created inside this location" />
                <Button Grid.Column="1" Content="Browse…" Click="OnBrowseClick" Margin="8,0,0,0" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="INITIAL SCENE" Style="{StaticResource SectionLabel}" Margin="0,0,0,6" />
            <TextBox x:Name="SceneNameBox" Text="Main" />
        </StackPanel>

        <TextBlock x:Name="ValidationText" Grid.Row="4" Foreground="#FF7A8A" Margin="0,14,0,0" TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" MinWidth="90" />
            <Button Content="Create Project" Style="{StaticResource AccentButton}" Click="OnCreateClick"
                    IsDefault="True" MinWidth="130" Margin="8,2,2,2" />
        </StackPanel>
    </Grid>
</Window>
